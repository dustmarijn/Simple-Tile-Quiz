import React from 'react';
import {Text, View} from 'react-native';

export default function Tile(){
    var indicationWord = 'hallo';

    return(
        <View>
            <Text>{indicationWord}</Text>
        </View>
    );
}